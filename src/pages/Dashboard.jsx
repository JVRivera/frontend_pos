import { Typography } from "@mui/material";

export default function Dashboard() {
  return <Typography variant="h4" sx={{ display: "flex", justifyContent: "center", mb: 2 }}>Bienvenido al sistema POS</Typography>;
}